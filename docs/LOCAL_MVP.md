# История локального dense RAG MVP

Текущая очередь и разрешённые действия — в [TASKS](TASKS.md); начало новой сессии — [HANDOFF](HANDOFF.md). Ниже сохранены этапы и команды разных дат. Упоминания 7B, 100 вопросов и Ready PR описывают историю/планы; они не разрешают повторять запуски или снимать Draft сейчас.

План от 23 сентября 2026 с учётом обратной связи руководителя. Цель: вопрос → top-5 пассажей → ответ настоящей локальной модели → оценка → сохранённый результат. Внутренний API не блокирует этот этап.

## Порядок работы

| Шаг | Работа | Критерий завершения |
|---|---|---|
| 0 | Ветка, AGENTS.md, .adr/, Draft PR | Опубликован PR с начальным описанием студента, assignee SPStan и label enhancement; reviewer resurrectiontent назначен либо ограничение явно отмечено |
| 1 | Данные MuSiQue и HotpotQA | Источники/ревизии/контрольные суммы сохранены; разобран один вопрос с ответом и supporting-пассажами |
| 2 | Подвыборки с seed=42 | По 500 вопросов, корпуса по правилам задания; повторяемые ID; тесты отсутствия дубликатов и наличия supporting-пассажей |
| 3 | Эмбеддер и небольшая локальная LLM | Реальный embedding и ответ; измерены память, скорость, контекст; выбор записан в ADR |
| 4 | Dense поиск и общий reader | Top-5 и ответ на 10–20 вопросах; правильный ответ не передаётся модели |
| 5 | Оценка и учёт | Проверены F1, EM, recall@5; JSONL, MLflow и Langfuse связаны общим run_id |
| 6 | Прогон на 100 вопросах MuSiQue | Сохранены ответы, ошибки, метрики, счётчики токенов и команда воспроизведения |
| 7 | Ревью | README и описание PR отражают фактическую работу; снят Draft; запрошено ревью |

## Обновление локального прогона 23 сентября 2026

Первый запуск нового reader (версия `hipporag2-musique-one-shot-v1`, сокращённый демонстрационный контекст и `num_predict=256`) выполнен на `debug10`, run_id `3865aaec-9cc3-4248-a4ac-8728017b2560`, Qwen2.5 3B Q4_K_M и BGE-M3 с прежним кэшем. После исправления inline-парсера: EM=0.100, token F1=0.167, recall@5=0.633 (шкала 0–1). Один ответ закончился `done_reason=length` без `Answer:` и остался пустым.

Повтор на полной демонстрации (`hipporag2-musique-one-shot-v2`, `num_predict=512`, `num_ctx=4096`) имеет run_id `03b21700-f44b-465d-a181-4eacebf4d59d`: EM=0.100, token F1=0.142, recall@5=0.633. Все 10 ответов извлечены, каждый завершён с `done_reason=stop`. Оба запуска используют те же ID и тот же упорядоченный top-5. Между ними менялись шаблон и лимит генерации одновременно, поэтому разницу F1 нельзя причинно приписать одному изменению. Оба — диагностические PoC, не независимый тест и не сравнение моделей.

## Manifest и embeddings без усечения

Новые запуски сначала записывают JSON-манифест со статусом, ожидаемыми ID, SHA256 подготовленных данных, model digests, Ollama/Python/runtime, Git commit и dirty-флагом, хэшами файлов кода, хэшем шаблона и параметрами. При завершении он получает итоговый статус и хэш JSONL; при сбое — тип и текст ошибки и имя частичного результата. Старые JSONL остаются неизменными и не имеют такого манифеста.

В новых embedding-запросах явно используется `truncate=false`. Старый кэш `indexes/dense/<dataset>-bge-m3.npz` не содержит этой настройки, поэтому он сохраняется как есть. Отдельный `*-notrunc-v2.npz` уже построен и проверен для 5500 пассажей MuSiQue; дальнейшие MuSiQue запуски используют этот кэш. Кэш не считается совместимым с другим набором данных или digest модели.

Исходная стоимость этого no-trunc кэша подтверждена manifest запуска `ed95bea9-9d11-4234-a80f-46ba3a8fd127`: полный корпус из 5500 пассажей был встроен за 78,764 с. При повторном использовании кэша время его чтения хранится отдельно; оценщик также показывает ID и время первоначальной сборки. В исходном запуске счётчики токенов и серверные длительности embedding API не сохранялись, поэтому они остаются `null` (неизвестно), а не восстанавливаются приблизительно. Runner проверяет завершённость источника и SHA256 его JSONL перед тем, как записать локальный sidecar provenance с контрольной суммой кэша.

Reader `hipporag2-musique-one-shot-v6` теперь использует без переформатирования шаблон из закреплённого upstream commit `1438aba3fc44ff10573e5a5e1e7cc3c7f9794aff`; исходные v5 прогоны сохранены как исторические результаты. Сообщения включают фиксированный few-shot пример и роли system/user/assistant/user. Эквивалентность prompt HippoRAG и dense проверяется тестом. Runner намеренно принимает только MuSiQue: шаблон для HotpotQA пока не выбран и не проверен. Генерация использует `temperature=0`, `seed=42`, `num_predict=512`, `num_ctx=4096`; seed снижает один источник вариативности, но не гарантирует полную детерминированность. Модель не получает ответ или supporting IDs. JSONL сохраняет сырой вывод и отдельно извлечённый финальный ответ; отсутствующая или неоднозначная метка означает пустой ответ, который остаётся в знаменателе и получает нулые EM/F1.

Оценщик привязывает оценку к `expected_question_ids` manifest, проверяет его статус, run ID и SHA JSONL. Он отклоняет неполный/смешанный прогон, неполную generation response и отсутствующую конфигурацию; `recall@k` ограничен первыми k пассажами. Runner сохраняет ответ и статус в частичный `.part` файл, затем выставляет `status=failed`, если Ollama не сообщил `done=true` и непустой `done_reason`; такой файл оценщик не принимает как полный. Формулы нормализации и F1 сверены с HippoRAG/SQuAD; версия оценщика записывается в метриках. Для старых запусков без manifest остаётся явный `--expected-view`; старые JSONL и оценки не переписываются автоматически.

## Контроль reader и оценщика, 24 сентября 2026

Первый технический вопрос v3 (`17d31655-dc99-436c-940f-eb5262959b74`) и запуск v4 (`00f91231-0799-470f-8f4d-f998ff988917`) сохранены локально, но последующая сверка точных границ upstream-примера выявила в них расхождения форматирования. Эти результаты помечены как промежуточные и не считаются проверкой финального шаблона.

После проверки границ строк выполнен один технический вопрос с reader v5, run `de5a0376-e3cd-4b4e-ad65-ec05c1aa8aae`, Qwen2.5 3B Q4_K_M, BGE-M3 и существующим no-trunc кэшем. Manifest имеет `status=completed`, Ollama вернул `done=true` и `done_reason=stop`; prompt занял 1181 токен при `num_ctx=4096`, генерация — 101 токен. Оценщик проверил пару JSONL/manifest и дал EM=0.000, token F1=0.000, recall@5=0.667. Это один вопрос и только техническая проверка, не оценка качества.

Этот run экспортирован без повторной генерации: общий `run_id` проверен в обоих сервисах, Langfuse trace `3e5360c0158f9ef486449aa3a604434c` содержит пять retrieved passages и usage 1181/101; MLflow run `6834ccbe0b1f4204815ed4b4328d3b3f` имеет статус FINISHED, те же метрики и JSONL, manifest и metrics как артефакты.

После перезагрузки Windows и повторного запуска сервисов свежие записи v5 проверены повторно. MLflow run по-прежнему FINISHED; три артефакта доступны, а скачанный JSONL совпадает с SHA из manifest. MLflow trace `tr-666a616e47ce8c3641cb8a4b82d60169` содержит root и стадии вопроса, query embedding, retrieval и generation. Langfuse trace `3e5360c0158f9ef486449aa3a604434c` сохранил те же пять типов observations, пять retrieved passages и usage 1181/101. Это подтверждает сохранность локальных записей после одного перезапуска.

## Debug10 с reader v5, 24 сентября 2026

После проверки одного вопроса выполнен полный инженерный прогон на 10 ID `debug10`: run `877f86c6-e9ff-43e1-80f9-d09962e62fe2`, Qwen2.5 3B Q4_K_M, BGE-M3, reader v5, top-5. Все 10 генераций завершились, кэш 5500 embeddings прочитан за 0,113 с; повторная индексация не выполнялась. Оценщик принял полный manifest и получил EM=0,100, token F1=0,190, recall@5=0,633 (шкала 0–1). Все supporting ID найдены у 3 из 10 вопросов; на двух из этих трёх reader всё равно ответил неверно. В вопросе о Katy Perry модель выбрала Betty Boo, хотя в top-5 были нужный пассаж про Michael Bublé и отвлекающий пассаж о Betty Boo; в вопросе о Vito Corleone ответила Michael Corleone при наличии обоих персонажей в контексте. Это похоже на ошибку выбора сущности среди связанных кандидатов, но не доказывает единственную причину. Это отладочный срез, уже использовавшийся ранее: сравнение с прежними prompt-версиями не устанавливает причинный эффект и не является независимой оценкой качества.

Run и метрики экспортированы с общим `run_id`: MLflow run `8f1bc01d85b9480798d334fdebc3481c` имеет статус FINISHED, JSONL, manifest и metrics сохранены как артефакты; MLflow trace `tr-fdb59a1312db1029a504eccd76b5e593` содержит общий root и фазы всех 10 вопросов. Langfuse trace `20389b0a8fe6e5708bf87481a72c4f50` проверен: 50 retrieved passages, usage генерации 15668 входных и 1126 выходных токенов. Это счётчики локальной Ollama-модели, не расход внутреннего API. Run относится к той же debug-подвыборке, что и baseline100; отдельный holdout не запускался. Текущий полный набор содержит 44 теста; `pip check` проходит.

Добавлены целевые тесты шаблона/парсера и ошибок оценщика. После финального шаблона v5 прошли 24 целевых reader/evaluator теста и все 42 теста репозитория; `pip check` также прошёл. Исторический run `06dcabef-de41-4815-a5ac-4bcb3eb01b11` заново проверен новым оценщиком: баллы не изменились (0.000/0.250/0.667). Его параметры сохраняют прежний prompt v2 без seed, поэтому старые условия не приписываются v5.

## Контрольный запуск после исправлений, 23 сентября 2026

Одновопросный запуск `ed95bea9-9d11-4234-a80f-46ba3a8fd127` успешно создал манифест и новый полный кэш без усечения: 5500 пассажей обработаны за 78,76 с. Для вопроса `3hop1__593288_720914_27537` оценщик дал EM=0,000, token F1=0,250, recall@5=0,667; генерация завершилась с `stop`, использовала 1178 входных и 158 выходных токенов. Это проверка сквозного пути, а не оценка качества модели. Запуск был сделан до добавления фазовых замеров в JSONL, поэтому времена query embedding, retrieval и wall-clock ответа в этой записи отсутствуют. Отчёт содержит `null` там, где исходные данные не позволяют восстановить величину; отдельные фазы появятся в следующих прогонах. Индексная фаза читается оценщиком из парного manifest-файла.

Повтор с уже построенным кэшем `06dcabef-de41-4815-a5ac-4bcb3eb01b11` подтвердил сбор фазовых данных: чтение индекса 0,096 с; query embedding — 20 токенов, 2,648 с на стороне клиента; retrieval — 0,015 с; генерация — 1178 входных и 158 выходных токенов, 2,709 с wall-clock и 2,707 с по Ollama; полный путь вопроса — 5,373 с. Ответ и метрики совпали с предыдущим одновопросным запуском (EM=0,000, F1=0,250, recall@5=0,667). Индексный rebuild в этом прогоне не происходил, поэтому его стоимость неизвестна для данного запуска и записана как `null`, а не ноль. Это проверка корректности учёта на одном вопросе, не измерение качества.

## Экспорт завершённого Dense RAG запуска

`scripts/track_dense.py` связывает завершённые JSONL, metrics и manifest по общему `run_id`, восстанавливает полный текст top-k пассажей из закреплённого корпуса, отправляет параметры/метрики/артефакты и trace со стадиями query embedding, retrieval и generation в локальные MLflow и Langfuse. Запись помечена как post-hoc экспорт: измеренные интервалы берутся из исходного прогона, а не из времени отправки trace. Локальные токены Ollama помечаются как usage, не как счёт за API. Перед запуском должны быть доступны локальные сервисы и `.env`; ключи не выводятся и экспорт Langfuse разрешён только на localhost. Экспорт run `06dcabef-de41-4815-a5ac-4bcb3eb01b11` проверен: MLflow run `5ee46c6f61cb43ef9d082f7accfc4676` имеет статус FINISHED, метрики EM/F1/recall@5 и три скачиваемых артефакта; JSONL после скачивания совпал с SHA из manifest. MLflow trace содержит root и четыре типа дочерних span с входами/выходами. Langfuse trace `f1a4fc02941fa8d6cda110e3187abf78` содержит общий `run_id`, пять полных retrieval-пассажей и usage генерации 1178 входных/158 выходных токенов. После `docker compose restart` Langfuse trace сохранился; после перезапуска MLflow server run, trace и артефакты читались, SHA JSONL совпал с manifest. Первая попытка экспорта прервалась из-за кодировки emoji в Windows-консоли; незавершённая запись оставлена со статусом FAILED, итоговый экспорт — отдельный завершённый run.

```powershell
.\.venv\Scripts\python.exe scripts\track_dense.py results\raw\dense-musique-06dcabef-de41-4815-a5ac-4bcb3eb01b11.jsonl
```

Скрипт проверяет появление MLflow trace и ожидаемых observations Langfuse; затем записи нужно проверить в обоих UI по `run_id`.

## Первый Dense RAG прогон

Из корня проекта запусти локальный PoC на первых 10 закреплённых вопросах MuSiQue:

```powershell
.\.venv\Scripts\python.exe scripts\run_dense.py --dataset musique --limit 10
```

Скрипт создаёт или использует локальный кэш эмбеддингов для общего корпуса из 5500 пассажей, получает top-5 для каждого вопроса через Ollama и передаёт reader только найденные пассажи и вопрос. Текущая версия `hipporag2-musique-one-shot-v6` использует точные system/few-shot сообщения закреплённого шаблона HippoRAG; сырой вывод хранится отдельно от извлечённого ответа. Поддерживается только MuSiQue. Upstream-файл и commit закреплены в [THIRD_PARTY_NOTICES.md](../THIRD_PARTY_NOTICES.md). В JSONL записываются ID, cosine scores, длительность, локальные счётчики токенов, параметры, версия и SHA256 сообщений reader. `labels.json` скрипт не читает. Первый запуск может занять несколько минут из-за эмбеддинга корпуса; кэш `indexes/dense/` и JSONL в `results/raw/` исключены из Git.

Результат помечается `mode=local-poc`; число локальных токенов не является расходом внутреннего API. Сам прогон не читает эталонные ответы и не считает качество.

После завершения генерации можно отдельно посчитать метрики:

```powershell
$runFile = Read-Host "Вставь путь JSONL из строки Results"
.\.venv\Scripts\python.exe scripts\evaluate_dense.py $runFile --expected-view debug10
```

Оценщик запускается только после генерации и читает `labels.json` отдельно: для ответа берётся максимум по эталону и алиасам, EM и token F1 используют нормализацию HippoRAG/SQuAD, recall@k — доля supporting IDs, найденных среди первых k результатов. Для новых запусков paired manifest задаёт полный упорядоченный список ID; оценщик проверяет статус, run ID и SHA JSONL и отклоняет неполный прогон или несогласованную конфигурацию. Пустой ответ (например, если маркер `Answer:` отсутствует или неоднозначен) получает ноль и остаётся в знаменателе. Для исторического JSONL без manifest укажи закреплённый view, например `--expected-view debug10`. Метрики сохраняются рядом с JSONL с суффиксом `.metrics.json`. Эталон и supporting IDs не передаются reader-модели.

На весь этап предварительно 20–30 часов активной работы. Скачивание, установка GPU-зависимостей и скорость генерации могут увеличить срок. При пяти часах в день: два рабочих блока примерно по два часа и час на понимание и документирование. Не ставить отметки о готовности по календарю.

## Данные и условия

Основная подвыборка — 500 вопросов на датасет. Существующие debug10, debug20, baseline100 и pilot200 остаются префиксами одного списка: все сохранённые локальные прогоны использовали только 10 вопросов debug10, и эти ID входят в baseline100. Поэтому baseline100 пригоден как инженерный smoke, но не как независимая оценка. Отдельный `holdout100` закреплён за индексами 400–499 и не пересекается с первыми 200 ID; запуск на нём не выполнялся. Поиск при оценке ведётся по общему зафиксированному корпусу, а не только по supporting-пассажам выбранного вопроса.

Сохраняем supporting-пассажи всех 500 вопросов и добавляем дистракторы до 5500. Если supporting-пассажей больше целевого размера, сообщаем это и пересматриваем размер явно. В Git — ID, код, тесты и небольшие сводки; сырые данные и индексы остаются локально. Перед публикацией производных материалов проверяются условия источника.

## Модели и ресурсы

Приоритет для эмбеддера — bge-m3 из задания. Кандидат для первого генератора — небольшой квантованный Qwen; Qwen2.5 3B Q4_K_M через Ollama рассматривается как стартовый вариант, а не утверждённый по измерениям выбор. Источники: [bge-m3](https://huggingface.co/BAAI/bge-m3), [сборка Qwen](https://ollama.com/library/qwen2.5:3b).

Проверяем версии и совместимость до установки. Размер файла модели не равен требуемой VRAM. Начинаем с одного запроса и малого batch. При необходимости сначала считаем и сохраняем embeddings документов/вопросов, выгружаем эмбеддер, затем запускаем генератор. Фиксируем фактический GPU/CPU режим, truncation, контекст и ограничения. Не меняем embedding-модель молча.

## Результаты и проверки

- Различаем mock, local-poc и основной benchmark; toy_score не переносится в научные таблицы.
- На вопрос сохраняются ID, retrieved IDs, предсказание, эталон для оценки, время, статус/ошибка, модель, настройки, токены и run_id. Ошибочные вопросы не исключаются молча из знаменателя.
- Счётчики токенов берём из реального ответа сервера; если недоступны, значение неизвестно, а оценка токенизатором явно помечается оценкой.
- Метрики проверяем на примерах с известным результатом. Seed генерации не объявляем гарантией полной GPU-детерминированности.
- Langfuse хранит трассу вопроса, MLflow — настройки/метрики/артефакты запуска, JSONL — подробные исходы. Общий run_id связывает записи.
- Проверяем чтение файла-артефакта MLflow и доступность прежних результатов после перезапуска. Перенос MLflow в Compose не является условием MVP.
- Низкий F1 не мешает завершению технического MVP, если подсчёт корректен и ограничения объяснены.

## После MVP

HippoRAG 2 на sample, затем RAPTOR на малом корпусе; LightRAG — расширение. Используем готовые пакеты/сервисы, добавляя необходимые адаптеры и оценку. Сравнения выполняются на одинаковых данных и моделях; несовместимости и ограничения маленького генератора сохраняются в отчёте.

Доску задач подключаем после её появления, до этого статус ведётся в docs/TASKS.md и PR. При готовности к ревью студент отправляет ссылку руководителю. Руководитель разрешил напоминать ему о ревью; автоматические сообщения и напоминания этим планом не создаются.

## Парное сравнение reader 3B/7B на сохранённых контекстах, 24 сентября 2026

Добавлен `scripts/replay_reader.py`, который проверяет завершённый manifest и SHA исходного dense run, а затем повторно запускает только генератор по сохранённым retrieved passages. Для пары исходный 3B run `877f86c6-e9ff-43e1-80f9-d09962e62fe2` и replay 7B `0fd4ab76-0923-4977-a01b-de4ca8f56e82` программно подтверждены равенство question IDs, упорядоченных top-5 объектов (включая текст и score), `top_k`, полного reader prompt SHA, версии шаблона и generation options. Таким образом, retrieval не повторялся и не смешивался с изменением генератора.

| Reader | EM | Token F1 | Recall@5 | Генерация всего | Output tokens/s |
|---|---:|---:|---:|---:|---:|
| Qwen2.5 3B Q4_K_M | 0,100 | 0,190 | 0,633 | исходный run | — |
| Qwen2.5 7B Q4_K_M | 0,200 | 0,218 | 0,633 | 29,965 с | 72,13 |

7B завершил все 10 ответов штатно; Ollama сообщил digest `845dbda0ea48ed749caafd9e6037047aa19acfcfd82e704d7ca97d631a0b697e`, `size_vram` 4 748 056 984 байт и runtime context 4096. Локальная генерация учла 15668 input и 1408 output tokens. Это отладочный debug10, а не независимый benchmark; небольшое изменение EM/F1 не позволяет заключить, что 7B лучше в целом. Recall не менялся, так как все контексты взяты из того же retrieval run. Повторные embedding/retrieval usage и длительности оставлены null; происхождение контекстов записано отдельно.

Run экспортирован и проверен: MLflow run `e294cf1b391d48aa80e516822d903727`, trace `tr-caf65669f15ec11d34f42bc9a187a727`; Langfuse trace `bb45a4a258ffa38bcc254836a96ec658` содержит 50 retrieved passages и usage 15668/1408. После добавления replay полный набор из 47 тестов прошёл, `pip check` чистый. На момент этого debug10 сравнения holdout и запуск на 100 вопросах не выполнялись.

## Baseline100: парный запуск Qwen2.5 3B/7B, 24 сентября 2026

Для следующего инженерного этапа разрешённый runner расширен до 100 первых ID закреплённого списка; использован `baseline100` ровно из `data/ids/musique_s500.json`. Run Qwen2.5 3B `d49b37ad-51b1-42d9-8162-fe6424bfea84` построил retrieval один раз, а Qwen2.5 7B `48c2bb0e-9af4-4891-9224-69268ce8e4bf` повторно использовал те же контексты. Скрипт попарной проверки подтвердил одинаковые 100 question IDs, упорядоченные retrieved records, top-k, prompt SHA/версию и generation options. Пересечение с `holdout100` равно нулю; первые 10 ID входят в debug10, поэтому baseline100 годится для инженерного smoke, но не для независимых выводов.

| Reader | EM | Token F1 | Recall@5 | Генерация Ollama | Output tokens/s | Токены input/output |
|---|---:|---:|---:|---:|---:|---:|
| Qwen2.5 3B Q4_K_M | 0,150 | 0,241 | 0,595 | 104,698 с | 134,97 | 154029 / 11211 |
| Qwen2.5 7B Q4_K_M | 0,090 | 0,177 | 0,595 | 264,675 с | 70,75 | 154029 / 15930 |

Каждый reader штатно завершил 100/100 ответов. Retrieval и prompt не менялись, а recall совпал; на этом baseline100 7B снизил EM на 0,06 и token F1 примерно на 0,064 при примерно в 2,53 раза большем времени генерации. Полный supporting coverage в top-5 получен для 23 вопросов: exact match среди них — 9 у 3B и 5 у 7B; при неполном coverage exact match сохранился для 6 и 4 вопросов соответственно. Эти агрегаты отделяют случаи, где retrieval не вернул всю gold-разметку, но не являются полной ручной классификацией причин ошибок. На baseline100 разумно оставить 3B для текущего локального этапа; это не свидетельство общего превосходства, поскольку подвыборка использовалась в отладке, и целевой API/модель ещё не проверялись. 3B получил 2614 query-embedding tokens и выполнил retrieval за суммарные 2,073 с; эти фазы в 7B replay намеренно записаны как `null`, поскольку повторно не исполнялись. Индексный cache read для 3B занял 0,118 с.

Оба запуска связаны с MLflow и Langfuse. MLflow: 3B run `f57fdb822d7c4fb59342c98f89808548` (FINISHED), trace `tr-32e987b255d996664165f3cb72866e1f`; 7B run `3f14d03f0bf246b192f53b84ee4d8acd`, trace `tr-0ecd9f1c1f72ca9a7edd3cdc5f2ec573`. Langfuse: 3B `9515f9df88cefd168cabce497155b102` проверен чтением всех 401 observations, 500 passages и usage 154029/11211; 7B `fd967e8f983354d58740727888962343` проверен exporter-ом, 500 passages и usage 154029/15930. Первоначальный экспортер 3B остановился на тайм-ауте своего 100-observation чтения, хотя trace был полным; исправлена cursor-pagination, добавлен тест. Holdout100, pilot200 и внутренний API не запускались.

Команды воспроизведения baseline и reader replay:

```powershell
.\.venv\Scripts\python.exe scripts\run_dense.py --dataset musique --limit 100 --generation-model qwen2.5:3b
.\.venv\Scripts\python.exe scripts\replay_reader.py results\raw\dense-musique-d49b37ad-51b1-42d9-8162-fe6424bfea84.jsonl --generation-model qwen2.5:7b
.\.venv\Scripts\python.exe scripts\evaluate_dense.py results\raw\dense-musique-d49b37ad-51b1-42d9-8162-fe6424bfea84.jsonl
.\.venv\Scripts\python.exe scripts\evaluate_dense.py results\raw\dense-musique-reader-replay-48c2bb0e-9af4-4891-9224-69268ce8e4bf.jsonl
```

## HippoRAG 2: локальный sample smoke, 24 сентября 2026

Официальный HippoRAG 2 закреплён на commit `1438aba3fc44ff10573e5a5e1e7cc3c7f9794aff` и установлен в отдельное временное Python 3.12.6 окружение; основная `.venv` не менялась. Запущен upstream sample (3 пассажа, 1 вопрос) через Ollama OpenAI-compatible API, Qwen2.5 3B и BGE-M3. Для OpenIE потребовался `response_format={"type":"json_object"}`; без JSON-режима модель не вернула ожидаемую структуру.

Построен граф с 30 узлами и 56 рёбрами. Поддерживающий passage найден на третьем месте, recall@5=1.0, но ответ Qwen оказался неверным (EM=0, F1=0). Отдельно посчитан usage на каждый passage: embedding и OpenIE-токены зафиксированы в [JSON-результате](../results/summary/hipporag2-sample.json). Повторный запуск переиспользовал passage/entity/fact embeddings и два chat cache результата; query embedding вызван повторно.

Windows-ограничение upstream обойдёно в project runner без изменения закреплённого исходника: для имени локальной рабочей папки используются безопасные внутренние метки, API получает исходные имена моделей. Установка остаётся в отдельной среде. Project runner и evaluator проверены; сводка, метрики и ограничения описаны в [результате runner](../results/summary/hipporag2-runner-smoke.json). Один sample не является сравнением систем.

Сохранённый HippoRAG sample post-hoc экспортирован в MLflow и Langfuse. В обеих системах trace связывается исходным `run_id`; проверены indexing, OpenIE, query embedding, retrieval с полными текстами пассажей и generation. MLflow run содержит JSONL, metrics и manifest артефакты; Langfuse проверил 3 retrieved passages и generation usage 1085/184. Trace IDs и ссылки записаны в summary JSON. Это локальный usage Ollama и не расход внутреннего API.

Для повторного запуска нужен upstream clone на закреплённом commit и отдельное Python 3.12 окружение. Команды ниже намеренно требуют явные пути к корпусу и вопросам и ограничивают число вопросов, чтобы случайно не запускать весь корпус при `--limit 1`:

```powershell
$hippoEnv = Join-Path $env:TEMP 'hipporag2-1438aba3-venv'
$hippoPy = Join-Path $hippoEnv 'Scripts\python.exe'
$hippoRepo = Join-Path $env:TEMP 'HippoRAG-1438aba3-src'
py -3.12 -m venv $hippoEnv
& $hippoPy -m pip install -r requirements-hipporag2.txt
git clone https://github.com/OSU-NLP-Group/HippoRAG.git $hippoRepo
git -C $hippoRepo checkout 1438aba3fc44ff10573e5a5e1e7cc3c7f9794aff
& $hippoPy scripts\run_hipporag.py --dataset sample `
  --corpus (Join-Path $hippoRepo 'reproduce\dataset\sample_corpus.json') `
  --queries (Join-Path $hippoRepo 'reproduce\dataset\sample.json') `
  --limit 1 --results-dir results\raw --storage-dir storage\hipporag2-sample `
  --max-new-tokens 512 --num-ctx 4096
```

## HippoRAG 2: общий reader и debug10, 25 сентября 2026

Чтобы сравнить retrieval без изменения reader, HippoRAG MuSiQue переключён на тот же закреплённый upstream-шаблон `hipporag2-musique-one-shot-v6`, обычный текстовый режим Ollama и общий `Answer:` парсер, что и Dense RAG. Тест проверяет равенство полных role/content сообщений; reader-запросы обходят SQLite-кэш HippoRAG, чтобы итоговый прогон подтверждал реальные генерации. JSON mode остаётся включён только для OpenIE, где он нужен структурному извлечению.

Завершён debug10 run `a38d6e57-3a33-4ece-aedb-390736ae3a0d`: 10/10 QA-запросов прошли через Ollama без cache hits, total usage — 15 891 prompt и 1 330 completion tokens. Reader использует Qwen2.5 3B Q4_K_M, `temperature=0`, `seed=42`, `num_predict=512`, `num_ctx=4096`; top-5 и модель те же, что в Dense debug10. Общий evaluator проверил completed manifest, JSONL hash и 10 ожидаемых ID: EM=0,300, token F1=0,380, recall@5=0,783. У одного ответа `done_reason=length` и нет маркера `Answer:`; он учтён пустым ответом в знаменателе. Артефакты run остаются в локальном `results/raw/`.

Граф и все сохранённые векторы переиспользованы: 5500 passages, 44 890 entities и 48 002 facts; новая индексация не выполнялась. Это сравнение на debug10 и не независимая проверка качества: вопросы использовались для отладки, а в OpenIE state есть 452 passage без сущностей и 35 без троек. Usage двух ранних прерванных попыток отсутствует; суммарная стоимость извлечения графа неизвестна. Первый общий-reader прогон был полностью cache replay и сохранён как отдельный результат; финальный run выше обходит QA cache и является проверкой свежих генераций.

В manifest runner дополнительно фиксирует SHA исходников/зависимостей, Ollama и Python версии, проверки закреплённых queries/corpus/labels и fingerprint index pipeline. Embeddings для новых индексов отправляются на native `/api/embed` с `truncate=false`; текст подготавливается как в upstream adapter (переводы строк заменяются пробелами). Исторический debug10 использовал прежний index. Для rebuild использован отдельный storage namespace; старый индекс и результаты сохранены.

Проверочный rebuild завершён на 25 сентября. Первый проход (`70c6c590-7c2e-4315-938f-af5fe73644fa`) остановился из-за HTTP 400 во время кодирования фактов; сохранённые passages и entities остались на месте. Добавлено деление отклонённого пакета и исправлена форма матрицы для одиночного query. Второй проход (`a341ae75-f211-4e27-9d27-eed92ff7dc0a`) полностью создал 48 000 fact vectors и граф, но затем выявил ту же ошибку формы single-query embedding; эти данные сохранились. Успешный run `9cac28e5-2885-4510-b227-ecba06f1b3de` загрузил готовый граф и завершил 10/10 retrieval/QA.

В rebuilt index находятся 5 500 passage vectors, 44 883 entity vectors, 48 000 fact vectors, 50 383 узла и 121 026 ребра; в итоговом manifest нет ошибок OpenIE. Reader и настройки совпадают с Dense v6: Qwen2.5 3B Q4_K_M, BGE-M3, top-5, `temperature=0`, `seed=42`, `num_predict=512`, `num_ctx=4096`. Метрики debug10: EM=0,300, token F1=0,380, recall@5=0,783; 9/10 ответов завершились с `stop`, один достиг `length` без `Answer:` и оценён как пустой ответ. Это диагностическая десятка, а не независимая оценка.

Финальный run переиспользовал весь embedding cache, поэтому `index_embedding_prompt_tokens=0` относится только к новым запросам этого run; исходная стоимость в его manifest отмечена как неизвестная, а не нулевая. По связанным manifest rebuild известны 684 560 prompt tokens для passages, 322 667 для entities и 1 179 570 для facts, вошедших в итоговый index. Ещё 135 382 известных prompt tokens потрачены на частичный fact embedding, который пришлось повторить; usage одного отклонённого embedding-запроса неизвестен. QA этого run использовал 15 891 prompt и 1 330 completion tokens; query embeddings — 241 prompt token. Cached OpenIE ответы не считаются новыми API токенами.

Run `9cac28e5-2885-4510-b227-ecba06f1b3de` экспортирован с тем же ID: MLflow run `012cfff24a814219a4a1e4ff07d512f5`, trace `tr-a21481936644b58d549bfecaaa0095f2`; Langfuse trace `6fd2c221dc7ffc89f8014316e6e63fd3`. MLflow показывает параметры и три метрики, а сохранённые JSONL, metrics и manifest скачаны обратно и побайтно совпали с локальными файлами. Langfuse подтвердил 50 retrieved passages и usage генерации 15 891/1 330. Эти traces являются post-hoc учётом локального Ollama run, не внутреннего API. Raw результаты и index остаются в `results/raw/` и `storage/`, в Git не добавляются.

Аудит локального `openie_state.json` для rebuilt index: у 454/5500 passages пустой `extracted_entities`, у 35 пустой `extracted_triples`, у 8 пусты оба поля. В manifest runner при этом `openie_extraction_failures=[]`. Это показатели покрытия сохранённого результата, а не подтверждённые ошибки генерации; причины пустых извлечений ещё нужно проверить на примерах. Закреплённые ID views сверены: `debug10` целиком входит в `baseline100`, а `holdout100` не пересекается ни с `debug10`, ни с `baseline100`; holdout — последние 100 ID закреплённого списка seed 42 и не запускался.

Небольшая выборочная проверка трёх примеров показывает разные случаи и не заменяет полную ручную разметку: у passage `Major League Baseball All-Star Game Most Valuable Player Award` список entities пуст, хотя сохранено 41 triples; у биографии `Anne Simonett` есть 6 entities и нет triples; у таблицы призовых UEFA Champions League пусты оба списка. Это основание сохранить аудит как следующий шаг, но не менять извлечение по этим примерам и не объявлять все пустые результаты ошибочными.

Read-only audit `scripts/audit_openie.py` создал public compact [report](../results/summary/hipporag2-openie-coverage-rebuilt-debug10.json) без текста passages и локальных путей. Он разделил 5500 documents на 446 entities-empty/triples-present, 27 entities-present/triples-empty, 8 both-empty и 5019 both-present. Среди локально просмотренных both-empty examples есть табличный `UEFA Champions League` и обычные narrative passages `Dissolution of the Soviet Union` и `John, King of England`; формат таблицы не объясняет все пустые извлечения. SQLite cache содержит 11 048 responses, включая 180 с `finish_reason=length` и 200 non-JSON, но хранит только request hash, response и metadata без prompt или passage ID. Его aggregates нельзя честно сопоставить конкретным documents. `openie_extraction_failures=[]` в final cache-replay означает только отсутствие новых зафиксированных ошибок этого запуска, а не чистоту раннего построения индекса.

По [ADR-0007](../.adr/0007-openie-extraction-audit-and-independent-evaluation.md), entity extraction и triple extraction учитываются отдельно. Runner для новых запусков записывает attempt-level статусы `valid_empty`, `valid_nonempty`, `parse_error`, `truncated`, `raw_response_missing`, `request_error` и `completion_unknown`. В manifest сохраняются passage/model/prompt fingerprints, hash и размер ответа, cache status, finish reason, retry link и usage с явным unknown; raw passage/prompt/response не сохраняются в attempt ledger. Вызов должен быть наблюдён на LLM layer: ответ из более высокого HippoRAG OpenIE result cache без связанной попытки gate не пропускает. Синтетические тесты проверяют статусы и gate. Чтобы gate принял индекс, нужны оба завершённых extraction этапа для каждого passage и ноль unresolved failures. Исторические cache rows и manifests не переписаны и не позволяют восстановить прошлые attempt статусы.

Кандидат независимой оценки — зафиксированные упорядоченные ID S500[200:300] в [candidate view](../data/ids/musique_independent100_candidate.json), hash списка `a026de236266ceb4ee7faa4cde5d031996ecfd74f4e6edd806a8b08cc493d42b`. Read-only сверка нашла ноль пересечений с 29 MuSiQue manifests и 4 raw result files без manifests. Candidate view позднее был запущен: Dense завершил 100 вопросов, а HippoRAG остановлен acceptance gate до QA; детали и замороженная конфигурация приведены в разделе candidate evaluation ниже. Целевой API/T4 и HotpotQA пока не проверены.

Для последующих локальных HippoRAG run runner сохраняет клиентское время query embedding, retrieval, generation и полного вопроса. Это измерения wall time конкретного запуска; они не восстанавливают неизвестную стоимость исходного построения уже существующего графа.

`--limit` разрешён до 20 вопросов. JSONL и manifest пишутся в игнорируемую `results/raw/`, локальное хранилище — в игнорируемую `storage/`; их нельзя коммитить. Основная `.venv` остаётся без HippoRAG.

## Ранние прерванные попытки HippoRAG на 10 вопросах

Dense reader v6 завершил технический прогон на первых 10 закреплённых MuSiQue ID (`0b7bac50-d529-48f8-85d0-47ccc5228b94`). HippoRAG запущен на тех же вопросах и полном корпусе из 5500 passages, с Qwen2.5 3B Q4_K_M, BGE-M3, top-5, `temperature=0`, `seed=42`, `num_ctx=4096` и лимитом генерации 512. Это пробный общий прогон, не оценка на 100 вопросах или сравнение с 7B.

Первый проход остановился, потому что пять NER-ответов Ollama не соответствовали ожидаемому формату. Project runner теперь один раз повторяет ошибочный ответ без чтения кэша, нормализует object-shaped entities и восстанавливает отсутствующий граф только когда обнаружены уже сохранённые passage-векторы. Повторный проход прошёл NER и начал извлечение троек, но был остановлен пользователем на 1349/5500. Частичные ответы остаются в локальном LLM cache и могут быть переиспользованы; полный граф, JSONL ответов и метрики пары не получены, поэтому выводы о качестве не делаются.

Прерванные локальные manifest отмечены `interrupted`. Runner сохраняет известные usage events при Ctrl+C, вместо оставления попытки в `initializing`. Эти исторические попытки были завершены более поздним графовым прогоном и общим-reader debug10, описанными выше; старые файлы и кэш сохранены.

## Candidate evaluation update, 25 сентября 2026

После protocol freeze Dense 3B выполнен на замороженном view S500[200:300], 100 вопросов: EM 0,110, F1 0,186, recall@5 0,606; run `98d8b412-cec6-45e4-9c14-0e4ad85855bb`. Результаты проверены по ordered IDs и SHA-256. Отдельная свежая сборка HippoRAG (`e78eff08-532a-40b3-a359-49a6b08b32a7`) завершила extraction и построение графа, но OpenIE gate остановил run до QA: 160 unresolved outcomes (122 truncation triples, 37 truncation NER, 1 request error NER). Индекс и логи сохранены для диагностики; он не является eligible benchmark index, ответов HippoRAG на candidate view нет.

Агрегаты, конфигурация, хэши и ограничения без сырых ответов находятся в [candidate summary](../results/summary/musique-independent-candidate-s500-200-299.json). Парное сравнение и bootstrap не выполнены. Candidate run показал достижение жёстких лимитов OpenIE (512 токенов NER, 2 048 triples). Исправлена ветка runner, пропускавшая retry при частично разбираемом truncated JSON; тест проверяет это условие. Read-only аудит сохранённых артефактов показывает путь к точечной коррекции OpenIE и сборке графа на клонированных векторах; деталей и оценки запросов — в [repair readiness report](../results/summary/hipporag-repair-readiness-candidate.json). До реализации безопасного clone/repair режима повторная индексация и LLM-вызовы не выполняются. Этот candidate run не использовал `holdout100`; исторический baseline100 по-прежнему включает debug10.
